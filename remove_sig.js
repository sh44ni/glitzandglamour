const fs = require('fs'); const path = 'src/contracts/templates/special-events-v1-contract-only.html'; let text = fs.readFileSync(path, 'utf8'); const start = text.indexOf('      <!-- CLIENT SIGNATURE BLOCK -->'); const endToken = '      </div>\n\n    </div>\n\n    <hr class=\"divider\">\n\n    </div>\n  </div><!-- /c-body -->'; const end = text.indexOf(endToken, start); if (start !== -1 && end !== -1) { const replacement = text.slice(0, start) + endToken; fs.writeFileSync(path, replacement); console.log('Successfully replaced file'); } else { console.log('Could not find boundaries', start, end); }
